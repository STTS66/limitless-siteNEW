{$SITE_HOST} {
    encode zstd gzip
    root * /srv

    handle /api/* {
        reverse_proxy backend:8080
    }

    handle {
        try_files {path} {path}/ {path}.html /index.html
        file_server
    }
}
